
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@drawable/background_gradient">

    <!-- Preview da câmera, inicialmente oculto -->
    <androidx.camera.view.PreviewView
        android:id="@+id/viewFinder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" /> <!-- Inicialmente oculto -->

    <!-- Botão para abrir a câmera -->
    <Button
        android:id="@+id/buttonOpenCamera"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@drawable/button_background"
        android:drawableTop="@drawable/codigo"
        android:padding="15dp"
        android:drawablePadding="-20dp"
        android:textColor="@android:color/black"
        android:text="" />

    <!-- Botão para ir para outra página -->
    <Button
        android:id="@+id/buttonGoToAnotherPage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="21dp"
        android:background="@drawable/button_background"
        android:drawableTop="@drawable/carinho"
        android:drawablePadding="-15dp"
        android:padding="10dp"
        android:text=""
        android:textColor="@android:color/black" />

    <!-- Imagem no canto inferior esquerdo -->
    <ImageView
        android:id="@+id/imageViewBottomLeft"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginStart="8dp"
        android:layout_marginBottom="8dp"
        android:src="@drawable/logo" />
</RelativeLayout>
